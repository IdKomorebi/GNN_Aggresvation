# 构建与交付检查

检查日期：2026-09-24。

- 从项目根目录执行 `PAPER_PYTHON=... bash paper/codex/V1_en/build.sh --figures` 成功，全部九张图重生成，LaTeX/BibTeX 编译完成。
- 最终 PDF 为 16 页，含 9 张图、8 张表和 43 条引用；检查了全部页面渲染，并对图形单独进行可视检查。
- 47 个 LaTeX 标签无重复；正文交叉引用及文献键均有定义。
- 最终 TeX 日志没有未定义引用、缺字、字体替代、Overfull 或超页浮动体警告。仍有少量 Underfull 提示，来自双栏文字或浮动体页面的空间分配；检查中未见内容裁切。
- `pdffonts` 确认 PDF 中字体全部嵌入，未使用 Type 3 字体。
- `pdfimages -list main.pdf` 无位图条目：热图单元格也按矢量绘制，论文插图不是 PNG 截图。
- 原 V4 表格的数据行数值序列与新版一致；十个目标、264 个字段—目标对，以及两个阈值下 223/113 个“单独安全、组合关键”字段计数与既有输出一致。
- 原稿来源目录的 48 个文件 SHA-256 记录在 `source_manifest.json`；原稿的已跟踪文件没有改动。工作区变更限于新增的 `paper/codex/`。
- 论文及绘图数据路径不包含 120/121 号实验；没有启动模型训练。

PNG 为屏幕预览文件；论文正式嵌入的是 PDF。HTML 图形预览引用本地 SVG，不依赖外部服务。

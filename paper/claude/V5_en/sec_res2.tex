%===============================================================================
\section{Results IV: The Amortized Attacker}\label{sec:res-est}
%===============================================================================

\paragraph{Fidelity}
Fig.~\ref{fig:estimator} compares the amortized estimates with retrained ground truth for all
sets of size $\le 3$. The estimate of $V$ is biased downward, visibly so for PJM, where the
training set is smallest; averaged over the ten targets the error is 0.026 with a signed bias of
$-0.024$, i.e.\ almost entirely common-mode. As predicted by~\eqref{eq:prop}, the field-level
risk is estimated more accurately than the set values it is built from: the mean absolute error of
$\hat M^{(2)}$ is 0.021 and its Spearman correlation with the exact $M^{(2)}$ across fields is
0.957 (Table~\ref{tab:ablation}). Certification recovers most of the exact risk from very few
retrained backgrounds: retraining the single best-scanned background per field yields a lower
bound equal to 93.4\% of the exact $M^{(2)}$ on average, three backgrounds yield 97.0\%, and on
RTS-GMLC and CAISO the top-3 ratio exceeds 99\% (Fig.~\ref{fig:estimator}c). For a field set of
35 candidates this replaces the $1+34+\binom{34}{2}=596$ backgrounds per field of an exhaustive
evaluation by three.

\paragraph{Amplification and dangerous backgrounds}
The profile of Section~\ref{sec:res-amp} is estimated with similar accuracy: the mean absolute error of
$\hat\Gamma^{(2)}$ is 0.017. Identifying the single worst background is harder---the best-scanned background is the worst
one for 47\% of the fields (17--78\% across targets), and the worst one is among the three best-scanned for 67\% and among
the five best-scanned for 74\%---but this matters little, because dangerous backgrounds come in families of nearly tied
members: retraining the three best-scanned backgrounds per field recovers 92.8\% of the total amplification.

\begin{figure}[!tbp]
\centering
\includegraphics[width=\textwidth]{fig7_estimator.pdf}
\caption{Amortized estimates against retrained ground truth. (a) $\hat V_y(S)$ for sets of size
$\le 3$ (random subsample). (b) $\hat M^{(2)}$ for every field and target. (c) Certified lower
bound over exact $M^{(2)}$ when the top-$k$ scanned backgrounds per field are retrained.}
\label{fig:estimator}
\end{figure}

\input{tables/tab_ablation}

\begin{figure}[!tbp]
\centering
\includegraphics[width=\textwidth]{fig8_ablation.pdf}
\caption{(a--c) Estimator comparison and ablation over ten targets (bars: mean; dots: data sets);
light grey: no per-set readout or no backbone; dark grey: one-seed ablations; blue: ours.
(d--f) Backbone scope on the four PJM/CAISO targets: inactive hidden units and effective
dimension, error of $V$, of marginals and of $M^{(2)}$, and certified ratios.}
\label{fig:ablation}
\end{figure}

\paragraph{What matters in the estimator}
Table~\ref{tab:ablation} and Fig.~\ref{fig:ablation}(a--c) separate the components. The largest
single effect is solving the readout per field set: with the same backbones, reading the
prediction off a shared output head gives an $M^{(2)}$ error of 0.057, against 0.021 for the
per-set readout, and a lower certified ratio. The second is a learned nonlinear representation:
without a backbone, a linear readout (0.144) or an in-set quadratic dictionary (0.077) is far
less accurate. Masking during pre-training is necessary---a backbone trained on fully visible
inputs is worse than the per-set readout on any other backbone (0.036, Spearman 0.928)---but the
masking distribution is not critical (Bernoulli versus uniform visible count: 0.024 versus
0.023). The numerical safeguards remove the overestimation of $M^{(2)}$ on real data (bias
$+0.009$ without them, certified top-1 ratio 0.825 instead of 0.936). Averaging three seeds
reduces the $V$ error from 0.030 to 0.026.

\paragraph{Why target-only masked pre-training adds little}
One result of the ablation is counter-intuitive: an untrained, randomly initialized backbone with the same per-set
readout estimates $V$ and $M^{(2)}$ about as well as the backbones pre-trained on the targets
(Table~\ref{tab:masking}). Three findings explain it (Fig.~\ref{fig:masking}).
\emph{(i) Collapse.} A backbone trained only to predict one to three targets compresses its hidden layer onto the few
directions that predict them on average over masks: the effective dimension of the representation (participation
ratio) is 1.60, against 3.30 for an untrained network. Restricting pre-training masks to one to four visible
fields, the size of audit queries, does not change this (1.65), so the cause is the narrow objective, not the mask
distribution. \emph{(ii) What the readout uses.} Truncating the features of each set to their $r$ leading principal
components shows that pre-training does find target-aligned directions---at $r=2$ its error is half that of untrained
features---but that the readout draws most of its accuracy from the remaining directions, where random ReLU features
serve as well (Fig.~\ref{fig:masking}a). A ridge readout on 256 random features is a random-feature approximation of a
kernel regression~\cite{rahimi2007random}, which is already adequate for a function of at most three fields learned
from several thousand rows. \emph{(iii) Set size.} The gap opens for larger sets: for sets of 16 fields the error of
untrained features is 0.074, against 0.027 for target-only and 0.023 for reconstruction pre-training
(Fig.~\ref{fig:masking}b; gradient-boosting ground truth). Audit queries are small, which is where pre-training matters
least.

\paragraph{Repairing it}
Adding the reconstruction term of~\eqref{eq:pretrain} restores the effective dimension (2.75) and lowers the error of
$V$ by 24\% (three seeds, sets of size $\le3$ and of size four) and that of the marginals by 18\%. At first, $M^{(2)}$ did
not improve (0.027 against 0.021). Because $\Mik$ is a maximum over marginals, its error is governed by the upper tail of
the marginal errors, not by their mean (Fig.~\ref{fig:masking}c), and the tail had a single cause: reconstructing the
heavy-tailed price and reserve columns produces units that are almost constant on the training rows except for spikes.
For the single-field sets of PJM reserve totals a few test predictions explode and $\hat V=0$ where the truth is about
0.3, and every field evaluated over that background is overestimated. Clipping predictions to the range of the target
on the training rows removes this failure without using any test information. With it, reconstruction pre-training
improves every metric over the target-only estimator used for the main results: the error of $V$ by 26\%, that of
$M^{(2)}$ by 19\% and that of $\Gamma^{(2)}$ by 11\%; the certified top-1 ratio rises from 0.934 to 0.961, the
worst background is among the three best-scanned ones for 71\% instead of 67\% of the fields, and scan--certify recovers
91\% instead of 88\% of the critical fields at $\tau=0.5$ without a margin. Concatenating the features of one
reconstruction backbone with those of an untrained one, as (ii) suggests, is as accurate with one trained backbone
instead of three ($M^{(2)}$ error 0.016, critical-field recall 93\%, means over three seeds of both backbones).
Decisions move less than estimates: at $\tau=0.7$ every estimator recovers 75--79\% of the critical fields, and even
the untrained backbone recovers 92\% at $\tau=0.5$. With
clipping in place, target-only pre-training no longer beats the untrained backbone on any metric: its earlier advantage
in certification came from being less prone to readout explosions, not from better information. The results of
Sections~\ref{sec:res-amp}--\ref{sec:res-def} were computed with the target-only estimator and are, in this respect,
conservative.

\input{tables/tab_masking}

\begin{figure}[!tbp]
\centering
\includegraphics[width=\textwidth]{fig_masking.pdf}
\caption{Why target-only masked pre-training adds little, and the repair (ten targets). (a) Error of $\hat V$ when the
backbone features of each set are truncated to their $r$ leading principal components (200 sets of size $\le3$ per data
set, four data sets). (b) Error of $\hat V$ against set size; shaded: gradient-boosting ground truth. (c) Errors of
reconstruction pre-training relative to the target-only estimator E, without and with clipping predictions to the
training range: set values, marginals, the largest overestimate of a marginal per field, and $M^{(2)}$.}
\label{fig:masking}
\end{figure}

Warm-start fine-tuning of the whole network for each set, the natural fast alternative to
retraining~\cite{sun2025warmstart}, is less accurate than the closed-form readout on nine of ten
targets (on 150 random sets per data set; e.g.\ 0.028--0.044 versus 0.013--0.021 on RTS-GMLC)
while being slower.



\paragraph{One pre-training for all target definitions}
In practice the auditor's target list and candidate pool change---a field is reclassified, a new
target is added. With masked reconstruction over all 53 columns of a market, one backbone serves
every such definition through the readout alone. Table~\ref{tab:backbone} compares it with
backbones supervised on each group's own targets. The all-column backbone is more accurate for
$V$ (0.040 versus 0.049) and slightly more accurate for $M^{(2)}$ (0.026 versus 0.029), and its
certified top-1 ratio rises from 0.906 to 0.944.

The mechanism is visible in the representation. A backbone that serves one or two targets leaves a
quarter of its hidden units inactive (24\%) and has an effective dimension of 1.45; one that must
reconstruct every column keeps 92\% of units active and has an effective dimension of 2.44. It has
to encode the latent operating state---load level, renewable output, congestion---that drives all
grid quantities, which is exactly the argument for multi-task representation
learning~\cite{caruana1997multitask,baxter2000inductive,maurer2016benefit}. Why $V$ improves more
than $M$ follows from~\eqref{eq:prop}: 89--98\% of the $V$ error is common-mode, and a better
representation mostly shrinks this common part, which cancels in the marginals anyway.

The last row of Table~\ref{tab:backbone} tests a target never seen in pre-training: the target
column is deleted from the data before pre-training. Set values and marginals are estimated as well
as with the all-column backbone, but $\hat M^{(2)}$ is inflated (bias $+0.028$), because the
maximum over many marginals picks up the few that are overestimated; the certified top-1 ratio drops
to 0.75. Retraining three backgrounds per field restores it to 0.966. Re-use for unseen targets
therefore requires certification---which is the pipeline we use anyway.

\input{tables/tab_backbone}

\paragraph{Cost}
\input{cost_text}

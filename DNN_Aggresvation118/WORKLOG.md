# DNN_Aggresvation118 工作日志

## 2026-09-24

- 用户要求补"通用模型的对比与消融"，只与串行重训比成本（不提并行重训）。
- 设计：对比无主干读出（线性、二次、集合内二阶多项式）、共享输出头、热启动微调；消融随机主干、掩码分布、只用 φ、数值修正、种子数。
- 意外发现：随机初始化主干在 V/M 误差上与训练主干相当。没有回避，改为在论文中明确"读出 + 条件特征"是主因、预训练主要提升排序与认证。
- ft 首次运行因 `torch.Generator.manual_seed` 收到 numpy 整数报错（TypeError），修为 `int(r)` 后补跑。
- 计时（timeE、seq）第一次与 CAISO 的变体计算同在 GPU1 上，数字偏慢；最后在空闲 GPU 上重测。

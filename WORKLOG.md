
---

## ★端到端验证（2026-07-31 凌晨）：k\*=6 **不成立**

用修好的 φ（`r93`，只修元组错配）在**真实机密字段**上重扫六阶并认证。
关键设计：复用 96 号**同一候选池**（sample_seed 960727、n_sample 150000×2），
唯一变量是 φ，构成配对对照。

### 扫描端：新 φ 确实报出更强的候选

| | 池内最大估计 syn | 估计 >0.10 个数 |
|---|---|---|
| 96 号老 φ | 0.1065 | 3 |
| 97 号新 φ | **0.1475** | **9** |

### 认证端：增益完全没有迁移

| | top 估计 | **top 认证** | 对照认证 | 精确率@0.10 | MW p | AUC | ρ(估计,认证) |
|---|---|---|---|---|---|---|---|
| 老 φ | 0.102 | **0.031** | 0.032 | 0/10 | 0.74 | 0.42 | −0.16 |
| 新 φ | 0.118 | **0.016** | 0.026 | 0/10 | 0.94 | 0.30 | −0.23 |

新旧 top 的认证值差异不显著（p=0.24）——两者都与噪声无异。
40 个认证过的六阶集合（新旧各 10 top + 10 对照）最大仅 0.088，中位 0.026，
**对照组与 top 组水平相当**。预注册判据（p<0.05 且 AUC>0.7）未达到。

### 三条结论

1. **k\* = 5 确认，不推进到 6。**
2. **★注入定标会显著高估真实能力。** 合成注入上 k=6 命中率 32%→75%（p<0.001，
   72 元组/格），迁移到真实机密字段后为零。合成信号是乘积型、强度 0.20–0.35，
   而真实六阶协同落在认证协议约 0.03 的噪声底附近——搜索再好也没有可找的东西。
   这条须写进 96 号表 4 的限定，也是对领域的警示：用合成/注入基准评估
   高阶交互发现是通行做法，而它会系统性地给出乐观的能力上界。
3. **元训练 bug 修复本身仍然成立**，只是价值不在推进 k\*：`noaug ≈ r93x` 坐实了
   93 号"增广修不好"是实现缺陷；修好后合成端增益显著、主业无退化。
   是一个正确的修复，只是这个数据集的六阶没有东西供它发挥。

## 灵敏度分析：k>6 还值不值得做（两数据集）

可检测下限 = 2×噪声底 ÷ 回收率；外推真实最强 = k=5 认证值/回收率，按认证版衰减比外推。

**PJM**

| 阶 | 回收率 | 噪声底 | 可检测下限 | 外推真实最强 | 余量 |
|---|---|---|---|---|---|
| 5 | 0.385 | 0.0350 | 0.182 | 0.400 | **2.2×** ✅ |
| 6 | 0.276 | 0.0315 | 0.228 | 0.314 | **1.4×** ⚠ |
| 7 | 0.046 | 0.0224 | 0.974 | 0.247 | 0.25× ✗ |
| 8 | 0.070 | 0.0200 | 0.571 | 0.194 | 0.34× ✗ |

**CAISO**

| 阶 | 回收率 | 噪声底 | 可检测下限 | 外推真实最强 | 余量 |
|---|---|---|---|---|---|
| 5 | 0.304 | 0.0350 | 0.230 | 0.342 | 1.5× ✅ |
| 6 | 0.248 | 0.0315 | 0.254 | 0.227 | 0.9× ✗ |
| 7 | 0.117 | 0.0224 | 0.383 | 0.150 | 0.4× ✗ |
| 8 | −0.007 | — | ∞ | 0.099 | 测不了 |

（k=8 的下限看似低于 k=7 是噪声：两个回收率都≈0，不应读作"八阶更好测"。）

**结论**：两条曲线在 k=5/6 之间交叉——真实强度随阶下降，可检测下限随阶上升。
1. **k≥7 两数据集都不必做**：差 3–4 倍，且瓶颈在真值端（受样本量限制），加算力无用；
   96 号字典对照已证明搜索端在 k=7 仍能 100% 命中，所以问题不在搜索。
2. **k=6 PJM 边缘（1.4×）、CAISO 已在线下（0.9×）**，与实测吻合（PJM 最强认证 0.088、
   对照最大 0.054，露头但分不开）。
3. **★不能说"没有协同"，只能说"审计不了"**：外推 k=7 真实最强约 0.25(PJM)/0.15(CAISO)，
   都在论文 0.10 显著阈之上。**存在但不可认证**——对安全结论是本质区别，
   论文"最危险的协同是低阶的"必须限定为"在可认证范围内"，否则会被读成"高阶安全"。

## 最后一个杠杆：降噪声底（进行中）

对照组认证均值 0.0315 而标准差仅 0.0154 ⟹ **噪声底大半是双重 max 的系统性正偏，
不是方差**。故 `paired_certify.py` 同时上两招：
配对选择（c\* 与 T\* 在 search 片选、只在 audit 片报增量）+ 多种子平均（R=3）。
同一批重训同时输出新旧两个统计量，偏差消除量可直接读出，零额外算力。

硬闸门（预注册）：先在 k=5 上跑，必须复现 93 号认证为真的 10 个协同
（≥8 个仍 >0.10 且 ρ≥0.8），否则停下查错，不得用于 k=6 判决。

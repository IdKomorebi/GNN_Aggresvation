# DNN_Aggresvation98 工作记录

## 2026-09-15 立项

**背景**：用户让 GPT 写了研究方案 `reports/inference_capability_shapley_research_plan.md`
（通用集合推断能力模型 → 字段 Shapley → 字段交互）。方案最后一条判断是
"先别写 Shapley，先确认 F_θ(S) 学得准不准、标签 R² 怎么来的、未见掩码能不能泛化"。

**本号任务**（用户原话要点）：
1. 理解两个项目（本仓库 69/75/91/93/97 号 + diantan 处置主干）的通用模型；
2. 看通用模型还能怎么优化、效果怎样；
3. 用实验检验 GPT 方案是否可行；
4. 检查数据集是否需要预处理（相同列、全 0 列）。

**GPU 约束**：最多用三张（0/1/2），留一张给别人。

## 做法

- 先建"并行专用重训"真值引擎，使 2^14 联盟全枚举真值在分钟级可得 → 精确 Shapley/交互基准。
- 同一套真值上横比 13 个候选价值模型，并直接检验 GPT 方案的 2ε 界、单调正则、交互指标、模型依赖型 SAGE 对照。

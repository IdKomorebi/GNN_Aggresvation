# DNN_Aggresvation114 工作记录

## 2026-09-23

用户意见：PJM 实际负荷的候选里应去掉负荷预测（两列意思相近、数值也接近）；按披露时点收窄候选池的方案"确实合理"，可以重训。

**设计**：
- 候选池按目标不同，所以按"目标 + 候选池"分三组，每组完整走一遍 111 号的共享流程。
- 目标副本的界定：与目标是同一物理量的事前估计（预测值）。这条和 FINAL_PROTOCOL §2 一致，不是临时加的规则。
- 集合规模取到 4，让 K=3 也有正式三攻击器口径。
- 只有一个目标的组，多目标 DNN 与单目标 DNN 结构完全相同，不重复训练。

**灰色地带**：PJM 发电总出力的最强单字段是负荷预测（0.974），CAISO 实际负荷的最强单字段是 SCE 分区负荷预测（0.823）。
它们与目标不是同一个量，按规则保留；但"意思相近"，所以补了一个不重训的敏感性分析：剔除后单字段暴露消失（降到 0.42 左右），
组合风险完全不变（15/15、18/18）。这条比主结果本身更有说服力，写进了结论三。

**运行**：这次用链脚本内部的 `$$` 写 PID，启动后 ps 核对，没有再出现重复任务。

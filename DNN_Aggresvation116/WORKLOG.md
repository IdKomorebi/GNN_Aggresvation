# DNN_Aggresvation116 工作日志

## 2026-09-24

- 用户指出 114 号的候选池可能漏字段。核对后确认：100 号协议的 12 个"机密列"从未进入输入，其中及时披露的 7（PJM）/ 11（CAISO）列本该是候选。
- 决定：补全候选、全部重训（规模 ≤4），并借机在新候选池上复核 115 号的"全字段主干 V 更准、M 持平"。
- 旧的全字段主干（75/95/100 号）以 12 个机密列为输出、不含它们作输入，无法覆盖新候选，因此改为"全部 53 列作输入的掩码重建"预训练（`src/bb116.py: train_mae`），
  并加做"留目标主干"（整列删去目标）检验对未见目标的可用性——这是用户关心的"目标重新定义时无需重训"的直接检验。
- 结果：结论方向不变且更强；全列主干 V 更准、M 也略好；留目标主干需要认证（前 3 个背景即恢复）。

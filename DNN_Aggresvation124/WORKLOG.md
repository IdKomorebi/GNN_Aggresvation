# DNN_Aggresvation124 工作日志

## 2026-09-25

- 用户要求回到"背景会不会放大字段风险"这一初心；GPT 意见中把 Γ = M^(K) − M^(0) 与 Top-r 背景作为正式输出、并画"固有—背景"风险图，这几点与初心一致，采纳。
  其余如"有效背景阶数"等新指标不采纳，避免继续发明分数。
- 全部用已有真值计算，不占 GPU（GPU 被他人的 vLLM 进程占满）。

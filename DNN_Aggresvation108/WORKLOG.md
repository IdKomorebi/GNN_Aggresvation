# DNN_Aggresvation108 工作记录

## 2026-09-18（A 档收尾）

按 107 号 GAPS.md 的 A 档三条执行，目标是让论文可以直接开写。

**A1 的一个更正**：动手前以为 top-3 认证要重训（107 号 GAPS 写了「约 2 分钟」），
实际检查 104 号 `build_mtable.py` 后发现 `top{1,2,3}_exact` 列已经是这三个见证背景在正式真值上的边际——
因为见证背景 |T| ≤ K ≤ 2，V(T) 与 V(T∪{i}) 都在 k3 真值表里。所以是纯表格重算，零算力。
本号仍重写了一遍计算（`interval_audit.py`）而不是直接读 104 的 CSV，是为了同时拿到上界校准所需的完整边际表。

**A2 的决策过程**：先查了 `train_l1_hi.py` 能否直接跑 CAISO——它没有 `--ds` 参数，
从 `ROOT/base.yaml` 读配置且依赖 69 号 `prepare_data`，是 PJM 专用路径。
移植成本高且会引入与冻结协议不同的数据链，所以选降级附录。
但没有停在「省算力」这个理由上，而是补了量化证据：元训练唯一占优的见证召回，
在主口径改用 top-3 认证后已被反超。这样降级的理由站得住。

**A3 的意外收获**：本来只想标注 K=3 口径不同，算完发现**攻击器口径差 > K2→K3 阶数增量**，
两个数据集一致。这比单纯的「标注一下」有价值得多，可以直接支撑正文「K=2 足够」的选择。

**诚实记录**：区间保守判定在 τ=0.9 上 precision 塌到 0.420（PJM）/0.657（CAISO），
因为全局 δ 对「V 接近 1 的窄区间」太宽。已写进 CHANGELOG，论文里 τ=0.9 要单独说明或改分层 δ。
